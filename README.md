# Stripe-RAK-App-Template
Basic RAK app with AppSettings and Permission Scheme


CLI Commands to manage Stripe app
```
Inside that directory, you can run several commands:

$ stripe apps start
Start a development server locally for viewing your app in the Stripe dashboard

$ stripe apps add view
Add a new view to your app

$ stripe apps upload
```
